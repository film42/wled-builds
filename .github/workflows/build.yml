name: Build WLED Firmware

on:
  workflow_dispatch:
    inputs:
      version:
        description: "WLED version to build (e.g. 0.15.4), or 'new' to build all missing"
        required: true
        type: string
        default: "new"
      source:
        description: "Target set to build"
        required: true
        type: choice
        options:
          - all
          - generic
          - quinled
        default: "all"

  schedule:
    - cron: "0 6 * * *"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build:
    name: build (${{ matrix.source }})
    runs-on: ubuntu-latest
    timeout-minutes: 180

    # One job per target set, so one failing doesn't hide or block the other.
    # Scheduled runs have no inputs and build every set.
    strategy:
      fail-fast: false
      matrix:
        source: ${{ fromJSON((inputs.source == '' || inputs.source == 'all') && '["generic", "quinled"]' || format('["{0}"]', inputs.source)) }}

    # Don't let a manual run and the scheduled run race on the same uploads
    concurrency:
      group: build-${{ matrix.source }}
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync

      - name: Install PlatformIO
        run: uv run pip install platformio

      # Per-env: build → attest → publish (all in Python)
      - name: Build, attest, and publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          R2_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          VERSION="${{ inputs.version || 'new' }}"
          if [ "$VERSION" = "new" ]; then
            uv run wled-build build-new --source "${{ matrix.source }}" --output build_output
          else
            uv run wled-build build "$VERSION" --source "${{ matrix.source }}" --output build_output
          fi

      - name: Upload audit trail
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: audit-${{ github.run_id }}-${{ matrix.source }}
          path: |
            build_output/**/audit/
            build_output/**/logs/
          if-no-files-found: warn

      - name: Summary
        if: always()
        run: |
          echo "## Build Summary (${{ matrix.source }})" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          find build_output -name "*.bin" -exec sh -c 'echo "$(sha256sum "$1" | cut -d" " -f1)  $(basename "$1")"' _ {} \; | sort >> $GITHUB_STEP_SUMMARY 2>/dev/null || echo "No binaries"
          echo '```' >> $GITHUB_STEP_SUMMARY
